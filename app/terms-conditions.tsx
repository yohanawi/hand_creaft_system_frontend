import InfoPageTemplate from '@/components/Common/InfoPageTemplate';
import { termsPageContent } from '@/constants/infoPageContent';

export default function TermsConditionsScreen() {
    return <InfoPageTemplate content={termsPageContent} />;
}
